<Window x:Class="Calculating_triangles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Калькулятор Треугольника" Height="450" Width="500"
        Background="#2196F3"
        ResizeMode="NoResize">       

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFD700"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Background="#FF3CC5FF">
        <StackPanel x:Name="InputPanel" VerticalAlignment="Center">
            <TextBlock Text="Введите длины сторон треугольника" Foreground="White"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBox x:Name="TextBoxA"/>
                <TextBox x:Name="TextBoxB"/>
                <TextBox x:Name="TextBoxC"/>
            </StackPanel>

            <Button x:Name="CheckButton" Content="Проверить" Click="CheckButton_Click" Background="#FFFFDE2C"/>
        </StackPanel>

        <StackPanel x:Name="ResultPanel" VerticalAlignment="Center" Visibility="Collapsed">
            <TextBlock x:Name="ResultTextBlock" Text="Полученный треугольник равносторонний"/>

            <Button x:Name="RestartButton" Content="Начать снова" Click="RestartButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
